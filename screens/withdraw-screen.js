const withdrawScreen = (mainScreen, stack) => {
    mainScreen.innerHTML = document.querySelector('.withdraw-screen-meta').innerHTML;

    stack.push('withdraw-screen');

    const root = mainScreen.querySelector('.withdraw-screen');}

console.log(stack);